
@{
    ViewBag.Title = "ProtectedView";
}

<div id="panel"></div>

@section Scripts {
    <script>

        $(() => {
            new Session(30);
            let panel = new AutoRefreshedPanel("panel", "/Home/GetData", 2);
        })
    </script>
}

